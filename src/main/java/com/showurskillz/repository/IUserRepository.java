package com.showurskillz.repository;

/**
 * Created by nitin on 10/28/2016.
 */
/*
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
import org.springframework.data.repository.query.Param;
import org.springframework.stereotype.Repository;
import com.showurskillz.model.User;

@Repository
public interface IUserRepository extends JpaRepository<User, Integer>
    {
        @Query("select * from User u where u.username = :email and u.password= :password")
        User findByEmail&Password(@Param("email")String email, @Param("password")String password);
    }
}*/
